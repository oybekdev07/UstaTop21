-- Create database
CREATE DATABASE IF NOT EXISTS ustatop_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
USE ustatop_db;

-- Create a user for the application (optional)
-- CREATE USER 'ustatop_user'@'localhost' IDENTIFIED BY 'your_password_here';
-- GRANT ALL PRIVILEGES ON ustatop_db.* TO 'ustatop_user'@'localhost';
-- FLUSH PRIVILEGES;
